{
  "name": "surmise",
  "version": "1.0.0",
  "private": true,
  "description": "Surmise blog application",
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "start": "npm start --workspace=api",
    "dev": "npm run dev --workspace=api",
    "build": "npm run build --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
